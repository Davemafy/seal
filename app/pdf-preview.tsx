'use client';
import {useEffect,useRef,useState} from 'react';import type {Claim} from '@/lib/types';
export default function PDFPreview({url,claims,active,anchors}:{url:string;claims:Claim[];active:string;anchors:React.RefObject<Record<string,HTMLElement|null>>}){
 const canvas=useRef<HTMLCanvasElement>(null);const [page,setPage]=useState(1);const [total,setTotal]=useState(1);const [error,setError]=useState('');
 useEffect(()=>{let cancelled=false;let task:{destroy:()=>void}|undefined;(async()=>{try{const pdfjs=await import('pdfjs-dist');pdfjs.GlobalWorkerOptions.workerSrc='/pdf.worker.min.mjs';const loading=pdfjs.getDocument({url});task=loading;const doc=await loading.promise;setTotal(doc.numPages);const p=await doc.getPage(page);const viewport=p.getViewport({scale:1.8});const c=canvas.current;if(!c||cancelled)return;c.width=viewport.width;c.height=viewport.height;await p.render({canvas:c,canvasContext:c.getContext('2d')!,viewport}).promise;}catch{if(!cancelled)setError('Could not render this PDF page. Extracted text is still available in claim rows.')}})();return()=>{cancelled=true;task?.destroy()}},[url,page]);
 return <><div className="pdf-toolbar"><button type="button" disabled={page<=1} onClick={()=>setPage(p=>p-1)}>← Previous</button><span>Page {page} of {total}</span><button type="button" disabled={page>=total} onClick={()=>setPage(p=>p+1)}>Next →</button></div><div className="preview-box"><canvas ref={canvas} style={{width:'100%',height:'auto',display:'block'}} aria-label={`Uploaded PDF page ${page}`}/>{claims.filter(c=>c.source_bbox&&c.page===page).map(c=><div key={c.id} className={`bbox ${active===c.id?'focused':''}`} style={{left:`${c.source_bbox!.x*100}%`,top:`${c.source_bbox!.y*100}%`,width:`${c.source_bbox!.width*100}%`,height:`${c.source_bbox!.height*100}%`}} ref={el=>{anchors.current[c.id]=el}}/>)}</div>{error&&<p role="alert">{error}</p>}</>
}
